namespace D365FOSecurityConverter.Models
{
    public enum LayerType
    {
        Role = 1,
        Duty = 2,
        Privilege = 3
    }

    public enum ProcessingDirection
    {
        Child = 0,
        Parent = 1
    }
}
